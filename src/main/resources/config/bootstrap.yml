server:
  port: 8888
spring:
  application:
    name: cloud-config-service
  cloud:
    config:
      fail-fast: true
      server:
        native:
          search-locations: classpath:/config/board-service
  profiles:
    active: native